# インターネットに公開する手順（GitHub Pages）

友達にもスマホで見てもらえるように、このサイトをインターネットに置く手順です。
**全部無料**でできます。所要時間は 20〜30分くらいです。

---

## はじめに知っておくこと

- GitHub Pages で公開すると、**URLを知っている人なら誰でも見られる状態**になります。
  「友達だけに見せたい」場合は、**URLを友達だけに教える**という運用になります。
- ただし `index.html` には「検索エンジンに載せない」設定を入れてあるので、
  Google などで検索してもこのサイトは出てきません。
- このサイトには個人情報もゲームのアカウント情報も一切入っていないので、
  見られて困るものはありません。
- **アカウント作成とパスワードの入力は、必ずご自身で行ってください。**
  （私＝Claude は、パスワードの入力やアカウント作成を代わりに行えません）

---

## ステップ1：GitHub のアカウントを作る

すでにアカウントを持っている場合は、ステップ2に進んでください。

1. ブラウザ（Edge や Chrome）を開きます。
2. アドレス欄に `https://github.com/signup` と打ち込んで Enter を押します。
3. 画面の指示にしたがって、次を入力します。
   - **Email**（メールアドレス）… 受信できるメールアドレス
   - **Password**（パスワード）… 自分で決めます。**必ずメモしておいてください。**
   - **Username**（ユーザー名）… 半角英数字。**これが公開URLの一部になります。**
     例：`akira-poke` にすると URL は `https://akira-poke.github.io/...` になります。
   - あとは画面の質問に答えていきます。
4. 登録したメールアドレスに GitHub から確認コードが届くので、画面に入力します。
5. 「Dashboard」や「Home」という画面が出れば、アカウント作成は成功です。

> **うまくいったかの見分け方**：画面の右上に自分のアイコン（丸い画像）が表示されていればOKです。

---

## ステップ2：置き場所（リポジトリ）を作る

「リポジトリ」＝ファイルを入れておく箱、だと思ってください。

1. 画面右上の **「＋」** マークをクリックします。
2. 出てきたメニューの **「New repository」** をクリックします。
3. 次のように入力・選択します。
   - **Repository name**（箱の名前）… `pokemon-candy-calc` と半角で入力
   - **Public / Private** … **必ず `Public` を選びます**
     （`Private` だと無料では公開できません）
   - **Add a README file** … チェックは**入れなくてOK**です
4. 画面をいちばん下までスクロールして、緑色の **「Create repository」** ボタンをクリックします。

> **うまくいったかの見分け方**：
> `（あなたのユーザー名）/ pokemon-candy-calc` と書かれたページに移動すればOKです。

---

## ステップ3：index.html をアップロードする

1. いま開いているページの真ん中あたりに
   **「uploading an existing file」** というリンクがあるので、それをクリックします。
   - もし見当たらない場合は、ページ上部の **「Add file」** ボタン →
     **「Upload files」** をクリックしてください。
2. 別のウィンドウでエクスプローラーを開きます。
   `PC` → `ローカルディスク (C:)` → `ユーザー` → `oauao` → `Claude` → `pokemon-candy-calc`
   の順に開きます。
3. その中の **`index.html`** を、GitHub のページの
   「Drag files here」と書かれた四角い枠の中へ **ドラッグ＆ドロップ** します。
   （`index.html` をマウスの左ボタンで押したまま、GitHub の画面まで引っぱって離す）
4. `index.html` という文字がGitHubの画面に出たら、
   ページ下部の緑色の **「Commit changes」** ボタンをクリックします。

> **うまくいったかの見分け方**：
> ファイル一覧に `index.html` が1行、表示されていればOKです。

---

## ステップ4：公開のスイッチを入れる

1. ページ上部のメニューにある **「Settings」**（歯車のマーク）をクリックします。
2. 画面の**左側のメニュー**を下にたどって、**「Pages」** をクリックします。
3. 「Build and deployment」という見出しの下にある
   **「Source」** が `Deploy from a branch` になっていることを確認します。
4. その下の **「Branch」** の欄で、
   - 左のドロップダウン（今は `None` になっています）をクリックして **`main`** を選びます
   - 右のドロップダウンは **`/ (root)`** のままでOKです
5. 右の **「Save」** ボタンをクリックします。

> **うまくいったかの見分け方**：
> 画面の上のほうに
> `Your site is live at https://（あなたのユーザー名）.github.io/pokemon-candy-calc/`
> というメッセージが出ます。
> **すぐには出ません。1〜5分ほどかかります。** 出ないときは、
> しばらく待ってからページを再読み込み（F5キー）してください。

---

## ステップ5：URLを確認して友達に送る

公開されるURLは次の形です。

```
https://（あなたのGitHubユーザー名）.github.io/pokemon-candy-calc/
```

1. そのURLをブラウザのアドレス欄に打ち込んで、サイトが表示されるか確認します。
2. スマホでも開けるか確認します（自分のスマホでURLを開いてみてください）。
3. 問題なければ、そのURLを友達にLINEなどで送ります。

---

## あとから中身を直したいとき

1. `https://github.com/（あなたのユーザー名）/pokemon-candy-calc` を開きます。
2. ファイル一覧の **`index.html`** をクリックします。
3. 右上の **鉛筆マーク（Edit this file）** をクリックすると、その場で編集できます。
4. 直したら、右上の緑の **「Commit changes...」** → **「Commit changes」** をクリックします。
5. 1〜2分待つと、公開中のサイトにも反映されます。

パソコン側の `index.html` を直した場合は、
ステップ3と同じ手順で **もう一度アップロードし直せば上書きされます**
（同じファイル名なら上書きになります）。

---

## 検索エンジンに載せたくなったら

`index.html` の上のほうにある次の1行を消してください。

```html
<meta name="robots" content="noindex" />
```

この行がある間は、Google などの検索結果には出てきません。

---

## 困ったときのチェックリスト

| 症状 | 確認すること |
|---|---|
| URLを開くと「404」と出る | ・`index.html` という**名前が合っているか**（`Index.html` などは不可）<br>・ステップ4の「Branch」が `main` になっているか<br>・公開直後は数分かかります。少し待って再読み込み |
| Pages の設定画面に `main` が出てこない | ステップ3のアップロード（Commit changes）が完了しているか確認 |
| 画面が真っ白 | `index.html` が途中までしかアップロードされていない可能性。もう一度アップロードし直す |
| 文字が四角や「？」になる | `index.html` を保存し直すときに文字コードが変わった可能性。元のファイルを再アップロードする |
